# Copy to .env and adjust values. Do not commit .env.

POSTGRES_USER=dtv2
POSTGRES_PASSWORD=dtv2_dev_change_me
POSTGRES_DB=dtv2
# Host port when multiple Postgres instances exist locally
POSTGRES_PORT=5432

# Browser UI for Postgres (docker compose service "adminer")
ADMINER_PORT=8080
